echo "🚀 Running full lint suite before push..."
npx nx run-many --target=lint --projects=shared-types,shared-utils,api,frontend

if [ $? -ne 0 ]; then
  echo "❌ Linting failed! Please fix the errors before pushing."
  exit 1
fi

echo "✅ All linting checks passed! Ready to push."
